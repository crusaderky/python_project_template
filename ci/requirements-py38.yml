name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.8
  - pytest
  - pytest-cov
  # code linters and static checkers (running only in the latest version of Python)
  - black
  - flake8
  - isort
  - mypy
  # TODO: add your own dependencies (no version)
  # - dep1
  # - pip:
  #   - dep2
