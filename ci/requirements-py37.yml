name: test_env
channels:
  - anaconda
dependencies:
  - python=3.7
  - pytest
  - pytest-cov
  # code linters (running only in the latest version of Python)
  - flake8
  - mypy
  # TODO: add your own dependencies (no version)
  # - dep1
  # - pip:
  #   - dep2
