name: TEMPLATE-minimal
channels:
  - conda-forge
  - defaults
dependencies:
  - pytest
  - pytest-cov
  # TODO: add your own dependencies =minimum supported version
  # - dep1=1.0
  # - pip:
  #   - dep2==1.0
