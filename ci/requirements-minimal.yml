name: test_env
channels:
  - anaconda
  - conda-forge
dependencies:
  - python=3.5.0
  - pytest=3.6
  - pytest-cov
  # TODO: add your own dependencies =minimum supported version
  # - dep1=1.0
  # - pip:
  #   - dep2==1.0
