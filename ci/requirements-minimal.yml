name: TEMPLATE
channels:
  - conda-forge
dependencies:
  - pytest
  - pytest-cov
  - packaging

  # TODO: add your own dependencies =minimum supported version
  # - dep1=1.0
  # - pip:
  #   - dep2==1.0
