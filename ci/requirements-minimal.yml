name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.6
  - pytest
  - pytest-cov
  # TODO: add your own dependencies =minimum supported version
  # - dep1=1.0
  # - pip:
  #   - dep2==1.0
