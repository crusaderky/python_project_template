name: TEMPLATE
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pytest
  - packaging
  - coverage

  # TODO: add your own dependencies =minimum supported version
  # - dep1=1.0
  # - pip:
  #   - dep2==1.0
