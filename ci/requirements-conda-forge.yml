name: test_env
channels:
  - conda-forge
dependencies:
  # python=3.7.1=h5001a0f_0 is broken
  - python=3.7.1=h0371630_3
  - flake8
  - pytest
  # TODO: add your own dependencies (no version)
  - pip:
    - coveralls
    - pytest-cov
