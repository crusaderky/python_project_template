name: test_env
channels:
  - conda-forge
dependencies:
  # python=3.7.1=h5001a0f_0 is broken
  - python=3.7.1=h0371630_7
  - pytest
  - coveralls
  - pytest-cov
  # TODO: add your own dependencies (no version)
  # - dep1
  # - pip:
  #   - dep2
