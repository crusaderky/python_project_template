name: TEMPLATE-lint
channels:
  - conda-forge
  - defaults
dependencies:
  - black
  - flake8
  - isort
  - mypy
  - pytest
  # TODO: add your own dependencies (no version)
  # - dep1
  # - pip:
  #   - dep2
